/**
 * Stats — reading statistics display.
 * Fetches GET /stats/me and shows:
 *   - unique surface forms read
 *   - unique lemmas read
 *
 * TODO: implement with data from StatsOut schema.
 */
export default function Stats() {
  return <div data-component="Stats">Stats — coming soon</div>;
}
